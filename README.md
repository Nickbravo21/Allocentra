# Allocentra
Allocentra is a standalone Java decision engine that allocates limited resources and budgets under real-world constraints. It runs locally as downloadable software, produces deterministic and explainable allocation decisions, and supports audit logging, scenario simulation, and partial funding.
